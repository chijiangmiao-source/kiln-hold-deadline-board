# 只启动前端与 API 两个服务；SQLite 数据由命名卷 kiln-data 持久化，
# API 容器重启后从同一卷继续读写，截止时刻不漂移。
services:
  api:
    build: ./api
    environment:
      ADDR: ":8080"
      DB_PATH: /data/kiln.db
    volumes:
      - kiln-data:/data
    ports:
      - "${API_PORT:-8081}:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/api/health"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s

  frontend:
    build: ./web
    environment:
      # compose 网络内通过服务名访问 API。
      API_TARGET: http://api:8080
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "${WEB_PORT:-8080}:5173"

volumes:
  kiln-data:
